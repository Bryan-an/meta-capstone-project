/** @type {import('prettier').Config} */
const prettierConfig = {
  plugins: ['prettier-plugin-tailwindcss'],
  singleQuote: true,
  trailingComma: 'all',
  // Add other Prettier options here if needed
};

export default prettierConfig;
